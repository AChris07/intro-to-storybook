import { FunctionComponent } from 'react';

const UnreadIcon: FunctionComponent = () => (
  <span className="icon has-text-info">
    <i className="fas fa-circle" />
  </span>
);

export default UnreadIcon;
